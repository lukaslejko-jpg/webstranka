name: Smart Music V87 compact sync handler
on:
  push:
    branches: [tesla-waze-v87-work]
    paths:
      - 'tesla-waze-preview/music-v87-compact-sync-handler.py'
      - '.github/workflows/music-v87-compact-sync-handler.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tesla-waze-v87-work
      - name: Apply V87 patch
        run: python tesla-waze-preview/music-v87-compact-sync-handler.py
      - name: Syntax check
        run: node --check tesla-waze-preview/app.js
      - name: Verify only compact sync wiring
        run: |
          python - <<'PY'
          from pathlib import Path
          s=Path('tesla-waze-preview/app.js').read_text()
          assert s.count("$('musicCompactSync').onclick=()=>connectYoutube()") == 1
          assert s.count('MUSIC_COMPACT_SYNC_V87') == 1
          assert 'function connectYoutube(){' in s
          assert "window.open(PROD_ORIGIN+'/api/music/google/start'" in s
          PY
      - name: Rebuild JS runtime payload
        run: |
          python - <<'PY'
          import gzip,base64,pathlib
          p=pathlib.Path('tesla-waze-preview/app.js')
          raw=p.read_bytes()
          out=base64.b64encode(gzip.compress(raw,mtime=0)).decode()
          pathlib.Path('tesla-waze-preview/app.js.gz.b64').write_text(out,encoding='ascii')
          assert gzip.decompress(base64.b64decode(out))==raw
          PY
      - name: Commit V87
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add tesla-waze-preview/app.js tesla-waze-preview/app.js.gz.b64
          git commit -m 'music: wire compact sync to OAuth V87'
          git push origin HEAD:tesla-waze-v87-work

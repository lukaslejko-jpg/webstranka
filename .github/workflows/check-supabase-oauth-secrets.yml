name: Check Supabase OAuth secret presence

on:
  push:
    branches:
      - debug/tesla-waze-decode-20260903
    paths:
      - .github/workflows/check-supabase-oauth-secrets.yml

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check booleans only
        run: curl -fsS https://dimvegkezslqjtsxdohp.supabase.co/functions/v1/twsecretscheck
